use playground::{datasets, format, restaurant};

fn main() {
    // datasets::mut_fn();
    // datasets::vec_fn();
    // datasets::hash_fn();
    // datasets::collect_fn();
    // // let l = longest_with_an_announcement("xx", "y", "z");
    // // println!("l = {}", l);

    // restaurant::customers::eat_at_restaurant();

    format::demo::run();
    format::debug::run();
    format::display::run();
}
